![https://www.youtube.com/watch?v=JXh9AQkKmsw&ab_channel=Dr.TreforBazett](https://www.youtube.com/watch?v=JXh9AQkKmsw&ab_channel=Dr.TreforBazett)
### Area and volume
Consider a solid $E$ which lies in $\mathbb{R}^3$ between the planes $x = a$ and $x = b$. Suppose the cross sectional area of this solid at $x$ is given by $A(x)$. Then the volume of this solid is
$$
\int\limits_{a}^{b} A(x) dx
$$ 
Why? Well, this is the same thing as summing the surface areas ($A(x)$) over the entire $a,b$ range. This would give volume. Now let $E$ lie above a rectangular region $R = [a, b] \times [c,d]$ and below some surface given by $z = f(x,y)$. Since 
$$
A(x) = \int\limits_{c}^d f(x,y) \, dy
$$
This means that the volume of $E$ will be given b
$$
\text{vol } E = \int\limits_a^b\left(\int\limits_c^d f(x,y) \,\,dy\right)\,dx
$$

![[douleintegrals.png]]

Where this entire block is $E$. The cross section area $A(x)$ is essentially "what is the the surface area at a point $x$" where this surface area is given by the integral between $c$ and $d$ of $y$ under this curve $f(x,y)$ at the specific point $x$.

Note that we can do the same process but instead integrate the surfaces in the other direction. This would look like
$$
\text{vol } E = \int\limits_c^d\left(\int\limits_a^b f(x,y) \,\,dx\right)\,dy
$$
This can be interpreted as the integral of the surfaces given by $A(y)$ over the interval $c\rightarrow d$. Note this only applies whenever the base is rectangular. 
***
## Example 11.1
Find the volume of the solid bounded above by the surface $z=xy$ that lies above the rectangle in the $xy$-plane given by $0 \leq x \leq 1$ and $1 \leq y \leq 2$ 

<div style="display: flex; justify-content: center;">
	<iframe
			src ="https://www.desmos.com/3d/xf7a5wwrcj"
			width = 900
			height = 300
 ></iframe>
</div>

We see that this is a rectangular region. Setting up the integral
$$
\int\limits_1^2 \int\limits_0^1xy \ dx \ dy
$$
Now, evaluate the internal integral
$$
\int\limits_0^1xy \ dx = \frac{y}{2}x^2 \Big|_0^1 = \frac{1}{2}y
$$
Now the outer integral with with this expression 
$$
\int\limits_1^2 \frac{1}{2}y \ dy = \frac{1}{2}\left(\frac{y^2}{2}\right)\Big|_1^2 = \frac{1}{2}\left(\frac{4}{2} - \frac{1}{2}\right) = \frac{3}{4}
$$
Thus the area of this region is $\frac{3}{4}$
***
# 11.1.1 Double Integrals
## 11.1 2D Partitions
Let $f(x,y)$ be a function over the rectangle $R = [a,b] \times [c,d]$. A partition of $R$ is a collection of rectangles $R_ij = [x_{i-1}, x_i] \times [y_{j-1},y_j]$ for which $a = x_0 < x_1 < \dots < x_{n-1} < x_n = b$ is a partition of $[a,b]$, and $c = y_0 < y_1 < \dots < y_{n-1} < y_n = d$ is a partition of $[c,d]$. Let ${\bf c}_{ij}$ be a point in the rectangle $R_{ij}$. Then the quantity
$$
\sum\limits_{i,j=1}^n f({\bf c}_{ij}) \Delta A_{ij}
$$
Where $\Delta A_{ij} = \Delta x_{i} \Delta y_j$ is the area of the rectangle $R_{ij}$ is called a Riemann sum of $f$ on $R$ corresponding to this partition of $R$. $f$ is integrable on $R$ if the limit of the Riemann sums as $(\Delta x_i \Delta y_j) \rightarrow (0,0)$ approach some real number. The limit of these Riemann sums is denoted by 
$$
\iint\limits_R f(x,y) \ dA
$$
## Theorem 11.1 
Suppose $D$ is a bounded subset of $\mathbb{R}^2$ that is contained in a closed rectangle $R = [a,b] \times [c,d]$. Let $f$ be a function defined on $D$. Assume $g$ is the extension of $f$ to $R$ defined by 
$$
g(x,y) = 
\begin{cases}
 f(x,y) & \text{if} (x,y) \in D \\
 0 & \text{else}
\end{cases}
$$
Then f is integrable over $D$ if and only if $g$ is integrable over $R$, in which case
$$
\iint\limits_R f(x,y) \ dA = \iint\limits_R g(x,y) \ dA
$$
### Rationale
Ok, so this is basically talking about how we can still integrate a 3D region when the base is not rectangular. We can essentially complete the region into a square and ignore the added parts by letting the function equal zero at those parts so that they don't contribute to the overall volume. 
***
## Example 11.2
Let $R$ be a triangle in the $xy$-plane whose vertices are $(0,0),(1,0)$ and $(1,1)$. Evaluate the volume of the solid bounded above the plane $z = x + y$ that lies above the region $R$.

![[triangleregion.png]]

First setup the internal integral
$$
\int\limits_y^1 x+y \ dx
$$
Essentially, we are finding the area at some "fixed $y$", so it really is just the region between the point $x=y$ and $1$. Evaluating this integral
$$
\int\limits_y^1 x+y \ dx = \frac{1}{2}x^2 + xy \Big|^1_y = \frac{1}{2} + y  - \frac{3}{2}y^2
$$
Next, setup the outer integral with this expression and evaluate
$$
\int\limits_0^1 \frac{1}{2} + y  - \frac{3}{2}y^2 \ dy = \Big[\frac{1}{2}y + \frac{1}{2}y^2 - \frac{1}{2} y^3 \Big|^1_0 = \frac{1}{2} \Big[ y + y^2 - y^3 \Big|_0^1 = \frac{1}{2}
$$
Thus the volume is $\frac{1}{2}$. Alternatively, you could setup the double integral as
$$
\int\limits_0^1 \int\limits_0^x x+y \ dy \ dx
$$
***
## Theorem 11.2 
Let $f$ be a continuous function on a rectangle $R$, then $f$ is integrable. 

Any continuous function is integrable
## 11.2 Zero area
Let $X$ be a subset of $\mathbb{R}^2$. We say $X$ has zero area if for every $\epsilon > 0$ there is a sequence of closed rectangles $R_1, R_2, \dots$ for which 
$$
X \subseteq \bigcup\limits_{n=1}^{\infty} R_n
$$
and the sum of the areas $R_n$'s is less than $\epsilon$.

Essentially, if we can cover a set of points in $\mathbb{R}^2$ which increasingly small rectangles whose areas add up to be less than any arbitrary positive $\epsilon$, then the thing has zero area since $\epsilon$ can be very small.
## Theorem 11.3
Let $f$ be a function that is bounded over a rectangle $R$ for which the points of discontinuity of $f$ in $R$ has zero area. Then $f$ is integrable over $R$
## 11.3 Elementary regions
A region $D$ in $\mathbb{R}^2$ is called elementary if it can be describes in one of the following ways
I.
$$
D = \{(x,y) \,|\, a \leq x \leq b, \delta_1(x) \leq y \leq \delta_2(x)\} 
$$
Where $\delta_i$ are continuous over $[a,b]$
II.
$$
D = \{(x,y) \,|\, \varphi_1(y) \leq x \leq \varphi_2(y), c \leq y \leq d\} 
$$
Where $\varphi_i$ are continuous over $[c,d]$
## Theorem 11.4 Fubini's Theorem
Let $f(x,y)$ be a function that is bounded on a bounded set $D$ of the $xy$-plane, and let $S$ be the set of all points of discontinuity of $f$ on $D$. Assume $S$ has zero area, and suppose every line parallel to the $x$-axis and every line parallel to the $y$-axis intersects $S$ in finitely many points. Then, $f$ is integrable over $D$ and 

1. If $D$ is of type I as described above, then 
$$
\iint\limits_D f \ dA = \int\limits_a^b \int\limits_{\delta_1(x)}^{\delta_2(x)} f(x,y) dy \ dx
$$ 
2. If D is of type II, then
$$
\iint\limits_D f \ dA = \int\limits_c^d \int\limits_{\varphi_1(x)}^{\varphi_2(x)} f(x,y) dx \ dy
$$
## Theorem 11.5 Properties of integrals
Suppose $f$ and $g$ are integrable functions over a subset $D$ of $\mathbb{R}^2$, and $c$ is constant. Then,

1. $f + g$ is integrable and
$$
\iint\limits_D (f + g) \ dA = \iint\limits_D f \ dA \iint\limits_D g \ dA
$$
2. $cf$ is integrable and 
$$
\iint\limits_D cf \ dA = c \iint\limits_D f \ dA
$$
3. If $f \leq g$ over $D$, then 
$$
\iint\limits_D f \ dA \leq \iint\limits_D g \ dA
$$
4. $|f|$ is integrable over $D$ and 
$$
\left|\iint\limits_D f \ dA\right| = \iint\limits_D |f| \ dA
$$
***
## Example 11.3
Suppose $D$ is the region enclosed by $y = x^2$ and $y = \sqrt{x}$. Evaluate the following integral
$$
\iint\limits_D (xy + y^2) \ dA
$$
First, define the region $D$. This is basically the region under the curve $y = \sqrt{x}$ and $y = x^2$ or in other words, the region where
$$
x^2 \leq y \leq \sqrt{x}
$$
This is type I from 11.3. Now, define the boundary for $x$ i.e. when the two conditions for $y$ intersect. This gives
$$
x^2 = \sqrt{x} \,\,\,\, \Rightarrow \,\,\,\, x=0 \text{ or } x=1
$$
So $0 \leq x \leq 1$. This means that the entire region $D$ will be
$$
D = \,{(x,y) \,|\, x^2 \leq y \leq \sqrt{x} \text{ and } \, 0 \leq x \leq 1 \,}
$$
So, evaluating the integral over this region
$$
\int\limits_0^1 \int\limits_{x^2}^{\sqrt{x}} (xy + y^2) \ dy \ dx = \int\limits_0^1 \int\limits_{x^2}^{\sqrt{x}} xy \ dy \ dx + \int\limits_0^1 \int\limits_{x^2}^{\sqrt{x}} y^2 \ dy \ dx
$$
For the first integral,
$$
\int\limits_0^1 \int\limits_{x^2}^{\sqrt{x}} xy \ dy \ dx = \int\limits_0^1 x\frac{1}{2}y^2 \Big|_{x^2}^{\sqrt{x}} \ dx = \int\limits_0^1 \left[\frac{1}{2}x^2 - \frac{1}{2}x^5 \right] \ dx = \frac{1}{2} \Big[\frac{1}{3}x^3 - \frac{1}{6}x^6\Big|_0^1 = \frac{1}{12}
$$
For the second integral
$$
\int\limits_0^1 \int\limits_{x^2}^{\sqrt{x}} y^2 \ dy \ dx = \int\limits_0^1 \frac{1}{3} y^3 \Big|_{x^2}^{\sqrt{x}} = \frac{1}{3} \int\limits_0^1 x^{\frac{3}{2}} - x^6 \ dx = \frac{1}{3} \Big[\frac{2}{5}x^{\frac{5}{2}} - \frac{1}{7}x^7 \Big|_0^1 = \frac{3}{35}
$$
For the total area
$$
\frac{1}{12} + \frac{3}{35} = \frac{71}{420} \approx 0.169
$$
***
### Remarks / interpretations of double integrals
- Density and surface area
Let $D$ be a region in $\mathbb{R}^2$. If $\delta(x,y)$ is the density of a thing metal surface placed at $D$, then the total mass of this surface is given by 
$$
\iint\limits_D \delta(x,y) \ dA
$$
When $\delta(x,y) = 1$, then we get the area of $D$ 
- Average temperature
Let $R$ be a region in $\mathbb{R}^3$ and $D(x,y,z)$ be a function that yields the temperature at a point in three dimensional space. The average temperature within $R$ is given by 
$$
\frac{1}{V}\iiint\limits_R D(x,y,z) \ dV
$$
Where $V$ is the total volume of the region.
- Center of mass / centroids
The center of mass $(\hat{x},\hat{y},\hat{z})$ of a region $R \in \mathbb{R}^3$ with a density function $\rho(x,y,z)$ is given by
$$
\hat{x} = \frac{1}{M} \iiint\limits_R x\rho(x,y,z) \ dV
$$
Where $M$ is the total mass (which can be found by integrating $\rho$ over the region $R$)
# Changing the order of integration
Sometimes we can use double integrals to evaluate iterated double integrals i.e. integrals of the form 
$$
\iint f(x,y) \ dx \ dy \,\,\,\, \text{ or } \,\,\,\, \iint f(x,y) \ dy \ dyx
$$
![https://www.youtube.com/watch?v=LUvynduoUX0](https://www.youtube.com/watch?v=LUvynduoUX0)
***
## Example 11.4
Evaluate the following integral
$$
\int\limits_0^1 \int\limits_x^1 e^{y^2} \ dy \ dx
$$
First, note
$$
\int e^{x^2} \ dx = \frac{\sqrt{\pi} \ \text{erfi}(x)}{2} + C
$$
[not good](https://en.wikipedia.org/wiki/Error_function), we can't calculate this. Instead, lets try to change the order of integration. First, analyze the region we are integrating over.

<div style="display: flex; justify-content: center;">
	<iframe
			src ="https://www.desmos.com/calculator/kzkugm5cu1"
			width = 900
			height = 300
 ></iframe>
</div>

It's this triangle between the line $y=1$ and $y=x$. Thus if we swap the order of integration then the integral becomes
$$
\int\limits_0^1 \int\limits_0^y e^{y^2} \ dx \ dy
$$
This can be integrated. First, evaluate the internal integral
$$
\int\limits_0^y e^{y^2} \ dx = xe^{y^2}\Big|_0^y = ye^{y^2}
$$
Now evaluate the outer integral
$$
\int\limits_0^1 ye^{y^2} \ dy
$$
Utilizing $u$-sub
- $u = y^2$
- $dy = \frac{du}{2y}$
$$
\int\limits_0^1 ye^{y^2} \ dy = \int\limits_0^1 ye^u \frac{du}{2y} = \frac{1}{2} \int\limits_0^1 e^u du = \frac{1}{2}\Big[e^{y^2}\Big|_0^1 = \frac{e-1}{2}
$$
So the answer is $\frac{e-1}{2}$
***
# 11.1.3 Triple integrals
## 11.4 3D partitions
Let $f(x,y,z)$ be a function over the closed box $B = [a,b] \times [c,d] \times [p,q]$. A partition of $B$ is a collection of rectangles $B_{ijk} = [x_{i-1}, x_i] \times [y_{j-1},y_j] \times [z_{k-1}, z_k]$ for which 

- $a = x_0 < x_1 < \dots < x_{n-1} < x_n = b$ is a partition of $[a,b]$,  
- $c = y_0 < y_1 < \dots < y_{n-1} < y_n = d$ is a partition of $[c,d]$. 
- $p = z_0 < z_1 < \dots < z_{n-1} < z_n = q$ is a partition of $[p,q]$. 

Let ${\bf c}_{ij}$ be a point in the box $B_{ijk}$. Then the quantity
$$
\sum\limits_{i,j=1}^n f({\bf c}_{ijk}) \Delta V_{ijk}
$$
Where $\Delta V_{ijk} = \Delta x_{i} \Delta y_j \Delta z_k$ is the volume of the rectangle $B_{ijk}$ is called a Riemann sum of $f$ on $B$ corresponding to this partition of $R$. $f$ is integrable on $R$ if the limit of the Riemann sums as $(\Delta x_i,\Delta y_j, \Delta z_k) \rightarrow (0,0, 0)$ approach some real number. The limit of these Riemann sums is denoted by 
$$
\iiint\limits_B f(x,y,z) \ dV
$$

Similarly to double integrals, we define the triple integral of a function $f$ over bounded solid $E$ by placing $E$ into a box $B$ defining $f$ to be zero outside $E$. Thus
$$
\iiint\limits_E f(x,y,z) \ dV = \iiint\limits_B f(x,y,z) \ dV
$$
Make box find volume of box by integrating its surfaces then integrate all these boxes for a total (i think idk)
## Theorem 11.6
Suppose $E$ is a bounded subset of $\mathbb{R}^4$ that is contained in a closed box $R = [a,b] \times [c,d] \times [p,q]$. Let $f$ be a function defined on $E$. Assume $g$ is the extension of $f$ to $R$ defined by 
$$
g(x,y) = 
\begin{cases}
 f(x,y) & \text{if} (x,y,z) \in E \\
 0 & \text{else}
\end{cases}
$$
Then f is integrable over $E$ if and only if $g$ is integrable over $B$, in which case
$$
\iiint\limits_E f(x,y,z) \ dA = \iiint\limits_B g(x,y,z) \ dA
$$
Same as before, just with boxes
## 11.5 Zero volume
Let $X$ be a subset of $\mathbb{R}^3$. We say $X$ has zero area if for volume $\epsilon > 0$ there is a sequence of closed boxes $B_1, B_2, \dots$ for which 
$$
X \subseteq \bigcup\limits_{n=1}^{\infty} B_n
$$
and the sum of the areas $R_n$'s is less than $\epsilon$. SAME AS ZERO AREA
## 11.6 Elementary solids
A region $E$ in $\mathbb{R}^2$ is called an elementary solid if it can be describes in one of the following ways
**I.**
- $z$ bounded by a function of $x,y$ 
- $y$ bounded by a function of $x$
- $x$ bounded by two numbers
		OR
- $z$ bounded by a function of $x,y$
- $y$ bounded by two numbers
- $x$ bounded by a function of $y$
**II.**
- $z$ bounded by to numbers
- $y$ bounded by a function of $z$
- $x$ bounded by a function of $y,z$ 
		OR
- $z$ bounded by a function of $y$
- $y$ bounded by two numbers
- $x$ bounded by a function of $y,z$
**III.**
- $z$ bounded by two numbers
- $y$ bounded by a function of $x,z$
- $x$ bounded by a function of $y$
		OR
- $z$ bounded by a function of $x$
- $y$ bounded by a function of $x,z$
- $x$ bounded by two numbers
## Theorem 11.7 Fubini's again
let $f$ be a bounded function on the subset $E \in \mathbb{R}^3$. Suppose $E$ is of type 1 case 1. Assume the set $S$ of discontinuities of $f$ on $E$ has zero volume. If every line parallel to the $x,y$ or $z$ axes intersect $S$ at finitely many points, then
$$
\iiint\limits_E f(x,y,z) \ dV = \int\limits_a^b \int\limits_{\beta_1(x)}^{\beta_2(x)} \int\limits_{\alpha_1(x,y)}^{\alpha_2(x,y)} f(x,y,z) \ dz \ dy \ dx
$$
Just adjust for whatever type $E$ is.
***
## Example 11.5
Evaluate the volume of the solid that lies in the first octant (all positive $\in \mathbb{R}^3$) and inside the cylinders $x^2 + y^2 = 1$ and $y^2 + z^2 = 1$

<div style="display: flex; justify-content: center;">
	<iframe
			src ="https://www.desmos.com/3d/afauxhcqtq"
			width = 900
			height = 300
 ></iframe>
</div>

We will do this utilizing triple integrals. The volume of a region $E$ in $\mathbb{R}^3$ can be calculated with the triple integral
$$
\iiint\limits_E 1 \ dV
$$
Which we can convert into an iterated triple integral using Fubini's theorem. Lets try the order
$$
dz \ dy \ dx
$$
For the first inner integral $dz$, we can "pick a fixed $x$ and $y$" and see the bounds that this gives for $z$, this yields
$$
\int\limits_0^{\sqrt{1-y^2}} 1 \ dz
$$
This essentially gives us a 1D stick from the bottom of the object at $0$ to the top $\sqrt{1-y^2}$. We can then 'slide' this stick through the base of this object, which is its projection onto the $xy$ plane  in order to obtain its volume. This projection is given by a quarter circle on the positive quadrant of the $xy$ plane.

![[circlregion.png]]

So this give us the following triple integral
$$
\int\limits_0^1 \int\limits_0^{\sqrt{1-x^2}} \int\limits_0^{\sqrt{1-y^2}} 1 \ dz \ dy \ dx
$$
Unfortunately, we end up having to do [trig sub](https://en.wikipedia.org/wiki/Trigonometric_substitution) in order to solve the central integral. Lets try to swap the order of $dy$ and $dx$ and see if the integral is simpler
$$
\int\limits_0^1 \int\limits_0^{\sqrt{1-y^2}} \int\limits_0^{\sqrt{1-y^2}} 1 \ dz \ dx \ dy
$$
This is a lot simpler. Evaluating the first integral
$$
\int\limits_0^{\sqrt{1-y^2}} 1 \ dz = z \Big|_{z = 0}^{z = \sqrt{1-y^2}} = \sqrt{1-y^2}
$$
Now the middle integral
$$
\int\limits_0^{\sqrt{1-y^2}} \sqrt{1-y^2} \ dx = x \sqrt{1-y^2} \Big|_{x=0}^{x = \sqrt{1-y^2}} = 1-y^2
$$
Note that this is not $\pm$ since we are in the first quadrant. Now the final integral
$$
\int_0^1 1-y^2 \ dy = y - \frac{1}{3}y^3 \Big|_{y = 0}^{y = 1} = \frac{2}{3}
$$
Thus the volume of this region is $\frac{2}{3}$
***
## Example 11.6
Find the volume of the solid that lies above the surface $z = x^2 + y^2$ and below the plane $z=1$

<div style="display: flex; justify-content: center;">
	<iframe
			src ="https://www.desmos.com/3d/pojswx2znd"
			width = 900
			height = 300
 ></iframe>
</div>

This thing. Ok so the order doesn't really matter here, lets setup a triple integral in the order $dz \ dy \ dx$. The lower bounds for $z$ are given by $x^2 + y^2$ and the upper bound is just $z=1$. The projection of this shape onto the $xy$ plane is a circle with radius $1$. The lower bound for $y$ is then given by $-\sqrt{1-x^2}$ and the upper bound will be $\sqrt{1-x^2}$. We can then integrate this from $x=-1$ to $x=1$. This gives us the following triple iterated integral
$$
\int\limits_{-1}^{1} \int\limits_{-\sqrt{1-x^2}}^{\sqrt{1-x^2}} \int\limits_{x^2+y^2}^{1} 1 \ dz \ dy \ dx
$$
For the first integral
$$
\int\limits_{x^2+y^2}^{1} 1 \ dz = z \Big|_{z=x^2 + y^2}^{z=1} = 1 - x^2 - y^2
$$
For the central integral
$$
\int\limits_{-\sqrt{1-x^2}}^{\sqrt{1-x^2}} 1 - x^2 - y^2 \ dy = \Big[ y - yx^2 - \frac{y^3}{3} \Big|_{y=-\sqrt{1-x^2}}^{y=\sqrt{1-x^2}} = 2\sqrt{1-x^2} - 2x^2\sqrt{1-x^2} - \frac{2}{3}(1-x^2)^{\frac{3}{2}}
$$
$$
\Rightarrow \sqrt{1-x^2}(2 - 2x^2 - \frac{2}{3}(1-x^2))
$$
Now for the final integral
$$
\int\limits_{-1}^{1} \sqrt{1-x^2}(2 - 2x^2 - \frac{2}{3}(1-x^2)) \ dx
$$
We have to do [trig sub](https://en.wikipedia.org/wiki/Trigonometric_substitution) with $x = \sin(t)$ and $dx = \cos(t) dt$. This gives us the integral
$$
\int\limits_{-\frac{\pi}{2}}^{\frac{\pi}{2}} \sqrt{1-\sin^2(t)}(2 - 2(\sin^2(t)) - \frac{2}{3}(1-\sin^2(t))) \cos(t) \ dt
$$
Apply the trig identity $\sin^2(x) + \cos^2(x) = 1$ and simplify
$$
\int\limits_{-\frac{\pi}{2}}^{\frac{\pi}{2}} \frac{4}{3} \cos^4(t) \ dt
$$
Apply the power reduction formula $\cos^2(x) = \frac{1 + \cos(2x)}{2}$ and linearity
$$
\frac{4}{3} \int\limits_{-\frac{\pi}{2}}^{\frac{\pi}{2}} \left(\frac{1 + \cos(2t)}{2}\right)^2 \ dt
\,\,\,\,\,\,\,\, \Rightarrow \,\,\,\,\,\,\,\,
\frac{1}{3} \int\limits_{-\frac{\pi}{2}}^{\frac{\pi}{2}} 1 + 2\cos(2t) + \cos^2(2t) \ dt
$$
And once again, apply the power reduction formula
$$
\frac{1}{3} \int\limits_{-\frac{\pi}{2}}^{\frac{\pi}{2}} 1 + 2\cos(2t) + \frac{1 + \cos(4t)}{2} \ dt
$$
Evaluate each integral individually
$$
\int\limits_{-\frac{\pi}{2}}^{\frac{\pi}{2}} 1 \ dt = t \Big|_{-\frac{\pi}{2}}^{\frac{\pi}{2}} = \pi
$$
$$
\int\limits_{-\frac{\pi}{2}}^{\frac{\pi}{2}} 2\cos(2t) \ dt = -\sin(2t) \Big|_{-\frac{\pi}{2}}^{\frac{\pi}{2}} = 0
$$
$$
\frac{1}{2} \int\limits_{-\frac{\pi}{2}}^{\frac{\pi}{2}} 1 + \cos(4t) \ dt = 1 - \frac{1}{4}\sin(4t) \Big|_{-\frac{\pi}{2}}^{\frac{\pi}{2}} = 0
$$
Putting everything together,
$$
\frac{1}{3} \int\limits_{-\frac{\pi}{2}}^{\frac{\pi}{2}} 1 + 2\cos(2t) + \cos^2(2t) \ dt
\,\,\,\,\,\,\,\, \Rightarrow \,\,\,\,\,\,\,\,
\frac{1}{3}\left[\pi + \frac{\pi}{2}\right] = \frac{\pi}{2}
$$
Thus the volume of this area is $\frac{\pi}{2}$.
***
